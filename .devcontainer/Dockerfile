# Use the development stage from the main Dockerfile
FROM fps-genie:development

# Copy any devcontainer-specific configurations if needed
# The development stage already includes all tools and dependencies

CMD ["bash"]
